#include "main.h"

/**
 * print_sign - Prints the sign of a number
 * @n: number to check
 *
 * Return: 1 if positive, 0 if zero, -1 if negative
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	else
		return (n);
}
